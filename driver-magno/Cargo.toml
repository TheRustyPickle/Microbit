[package]
name = "driver-magno"
version = "0.1.0"
edition = "2024"

[lib]
bench = false
test = false

[dependencies]
cortex-m = { workspace = true, optional = true }
embedded-hal = { workspace = true, optional = true }

[features]
default = []
hal = ["dep:embedded-hal"]
raw = ["dep:cortex-m"]
